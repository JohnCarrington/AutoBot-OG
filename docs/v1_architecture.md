# AutoBot-OG — v1 Architecture

_Placeholder — full design to be filled in by the user._

Scope: GBPUSD only. Three strategies (Bollinger Reclaim / EMA Continuation / Liquidity Sweep) gated by a regime engine (H1 primary, M5 validation, hysteresis). See `MODULE.md` in each `src/` package for boundaries.
